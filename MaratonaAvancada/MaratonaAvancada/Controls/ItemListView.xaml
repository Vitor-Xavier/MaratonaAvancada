<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:MaratonaAvancada.Controls"
             x:Class="MaratonaAvancada.Controls.ItemListView"
             x:Name="itemListView">
  <ContentView.Content>
        <StackLayout Orientation="Horizontal" BindingContext="{x:Reference itemListView}">
            <controls:CircleImage Source="{Binding AvatarSource}" HeightRequest="80"/>
            <StackLayout>
                <Label Text="{Binding Title}" FontAttributes="Bold"/>
                <controls:DescriptionLabel Text="{Binding Description}" MaxLength="{Binding MaxLength}"/>
            </StackLayout>
        </StackLayout>
    </ContentView.Content>
</ContentView>